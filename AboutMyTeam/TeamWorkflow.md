+++ workflowนี้ใช้ai gen และปรับแต่ง +++
---

สมาชิกทีม leclerc
1.ปาณวัฒน์ จันทร์ทองหลาง 663380390-4
2.เกรียงไกร ประเสริฐ 6633800616-4
3.ปิติ มูลเทพพิชัย 663380045-1

# 🚀 **GitHub Workflow (กลุ่ม 3 คน)**

## ✅ 1) สร้าง Repository บน GitHub

ชื่อโปรเจกต์:

```
SortAlgo
```

## ✅ 2) สร้าง Branch ให้แต่ละคน

แนะนำชื่อ branch ตามหน้าที่:

| คน         | หน้าที่                | Branch         |
| -------    | -------------------- | -------------- |
| panawat    | Bubble Sort          | `panawat_390-4`  |
| kriangkrai | Quick Sort           | `kriangkrai_616-4`   |
| piti       | Main program + Merge | `piti_045-1` |

แต่ละคน clone โปรเจกต์:

```
git clone https://github.com/kku-computer-science/configuration-management-using-git-leclerc/repo.git
cd Project/SortAlgo
```

แล้ว checkout ไป branch ของตัวเอง

---

## ✅ 3) แต่ละคนทำงานตามไฟล์ของตัวเอง

### 👤 panawat

สร้างไฟล์:

```
src/BubbleSort.java
```

แล้วเขียนโค้ด BubbleSort

Commit:

```
git add .
git commit -m "Add BubbleSort class"
git push
```

---

### 👤 kriangkrai

สร้างไฟล์:

```
src/QuickSort.java
```

Commit:

```
git add .
git commit -m "Add QuickSort class"
git push
```

---

### 👤 piti

สร้างไฟล์:

```
src/MainProgram.java
```

Commit:

```
git add .
git commit -m "Add MainProgram"
git push
```

---

## ✅ 4) piti Review + Merge Pull Request

* ตรวจโค้ด
* ตรวจว่าไม่มี conflict
* กด **Merge**

หลัง merge เสร็จ โปรเจกต์จะรวมโค้ดของทุกคนเข้าด้วยกัน

---

## ✅ 5) Sync งานของทุกคน

หลัง merge ทุกคนต้องอัปเดตโค้ดของตัวเองด้วย:

```
git checkout main
git pull origin main
```

---

# 📌 Workflow (สั้น ๆ แบบสรุป)

```
1. สร้าง repo
2. สร้าง branch แยกกันทำงาน
3. แต่ละคนเขียนไฟล์ของตัวเอง
4. push งานขึ้น branch
5. Review + Merge
6. ทุกคน pull งานรวม
```


